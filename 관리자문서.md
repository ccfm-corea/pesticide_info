# 앱 관리자 문서

**가농인증 허용 농약 조회와 방제력 작성**
가톨릭농민회 과수위원회 · 최종 갱신 2026-08-02

---

이 문서는 앱을 **고치거나 점검해야 할 사람**을 위한 것입니다.
평소 자료 갱신만 하시는 분은 `README.md` 한 장이면 충분합니다.

---

## 목차

1. [전체 그림](#1-전체-그림)
2. [파일과 저장 위치](#2-파일과-저장-위치)
3. [자료가 흘러가는 여섯 단계](#3-자료가-흘러가는-여섯-단계)
4. [무엇을 왜 걸러내는가](#4-무엇을-왜-걸러내는가)
5. [고칠 일이 생기면 어디를 보나](#5-고칠-일이-생기면-어디를-보나)
6. [안전장치](#6-안전장치)
7. [증상별 대처](#7-증상별-대처)
8. [고친 뒤 반드시 확인할 것](#8-고친-뒤-반드시-확인할-것)
9. [설계할 때 정한 원칙](#9-설계할-때-정한-원칙)
10. [남아 있는 한계](#10-남아-있는-한계)

---

## 1. 전체 그림

```
농촌진흥청 농약안전정보시스템
   │  (사람이 엑셀 내려받아 시트에 붓기 — 연 1~2회)
   ▼
구글 스프레드시트  ← 조직 드라이브. 갱신은 여기서만.
   │  (앱이 열 때마다 자동으로 요청)
   ▼
index.html  ← GitHub Pages. 한 파일이 전부.
   │
   ├─ 왼쪽: 허용 농약 조회
   └─ 오른쪽: 방제력 작성 → JPG · PDF · 엑셀
```

**서버가 없습니다.** 계산과 화면 구성이 전부 사용자 휴대전화·PC 안에서 일어납니다.
생산자가 적은 내용도 그 기기에만 남고 어디로도 전송되지 않습니다.

| 구성 요소 | 위치 | 바꿀 일 |
|---|---|---|
| 원본 자료 | 조직 구글 드라이브 시트 | 연 1~2회 |
| 앱 | GitHub 저장소 `index.html` | 오류·개선 때만 |
| 사용자 기록 | 각자 기기(localStorage) | 손댈 수 없음 |

---

## 2. 파일과 저장 위치

### 저장소

```
index.html      앱 전체 (HTML + CSS + JS 한 파일, 약 1,260줄)
README.md       자료 갱신 안내 — 실무자용
관리자문서.md    이 문서
```

빌드 과정이 없습니다. `index.html`을 고쳐 커밋하면 1~2분 뒤 반영됩니다.

### index.html 안쪽 구조

| 줄 범위 | 내용 |
|---|---|
| 1~280 | CSS |
| 281~380 | 화면 뼈대(HTML) |
| 382~400 | 저장소 접근, 기본 상수 |
| **402~420** | **시트 주소와 요청문 — 가장 자주 볼 곳** |
| 421~470 | 주소 변환, CSV 해석 |
| 468~520 | 등급 순서, 판정 기준(항생제 등) |
| 523~600 | **`buildAll` — 자료 가공의 심장** |
| 600~690 | 자료 받기, 안전장치 |
| 690~860 | 필터·조회 결과 그리기 |
| 865~985 | 방제력 |
| 986~1180 | 인쇄물·JPG·엑셀 만들기 |
| 1181~끝 | 화면 전환, 이벤트 연결 |

줄 번호는 고치면 밀립니다. **찾을 때는 함수 이름으로 검색**하십시오.

### 브라우저에 저장되는 것

| 이름 | 내용 | 지워지면 |
|---|---|---|
| `gano.csv` | 마지막으로 받은 농약 자료 원문 | 다음 접속 때 다시 받음 |
| `gano.plan` | 작성 중인 방제력, 생산자 정보 | **생산자 작업 내용이 사라짐** |

`gano.plan`의 구조를 바꾸면 기존 사용자의 저장분이 깨질 수 있습니다.
`loadPlan()` 안에 옛 자료를 보정하는 대목이 있으니 그 방식을 따르십시오.

---

## 3. 자료가 흘러가는 여섯 단계

### 1단계 — 주소 만들기 `csvUrls()`

`SHEET_URL`에 넣은 시트 주소로 두 가지 주소를 만들어 **앞에서부터** 시도합니다.

1. **걸러 받기** — 구글에 `TQ` 요청문을 보내 필요한 것만 받음 (약 0.9MB)
2. **통째로 받기** — 1이 막히면 시트 전체를 받아 앱이 직접 거름

`SHEET_URL`에 이미 `output=csv`가 들어간 「웹에 게시」 주소를 넣으면 그것을 그대로 씁니다.

### 2단계 — 요청문 `TQ`

```
select C,D,E,H,F,G,T,U,K,I,J,M,N,P,R,S
where C matches '사과|배|복숭아|자두|단감|감'
  and K matches '살균|살충|균충|생조'
label C '작물명', D '적용병해충', …
```

- `C,D,E…`는 **엑셀 원본의 열 위치**입니다. C=작물명, K=용도, R=안전사용시기.
- `label`이 있어 응답에 머리글이 붙습니다.
- **생조(생장조정제)를 일부러 받아옵니다.** 목록에는 안 넣지만
  「목록에서 제외된 항생제 및 호르몬제(생조) 성분」 안내에 이름을 적기 위해서입니다.
- 제초제는 받지 않습니다. 성분이 37종으로 많아 화면 안내에 적지 않기로 했기 때문입니다.
  받아오지 않으므로 「제외된 …」 안내에도 나오지 않습니다.

> **엑셀 열 순서가 바뀌면 `TQ`의 알파벳을 전부 고쳐야 합니다.**
> 갱신 뒤 화면이 이상하면 이것부터 의심하십시오.

### 3단계 — CSV 해석 `parseCSV()`

직접 만든 해석기입니다. 따옴표로 감싼 칸, 칸 안의 쉼표·따옴표·줄바꿈을 모두 처리합니다.
실제 자료에 `응애(사과응애, 점박이응애)`처럼 쉼표가 든 병해충명이 96건 있어 필요합니다.

### 4단계 — 열 찾기

응답 첫 줄에서 `작물명`을 찾아 머리글 위치를 잡고, 이름으로 열 번호를 구합니다.
머리글이 없으면 **열 개수로 판단**합니다 — 20개 이상이면 원본 전체(`RAW`), 아니면 걸러 받은 것(`ORDER`).

### 5단계 — 가공 `buildAll()` ★ 핵심

한 행씩 아래 순서로 처리합니다. **순서가 결과를 좌우하니 함부로 바꾸지 마십시오.**

```
① 작물 확인      CROPMAP에 없으면 버림 (감 → 단감으로 합침)
② 성분명 뽑기     품목명에서 제형을 떼어냄
                 "가스가마이신 액제" − "액제" → "가스가마이신"
③ 호르몬제 걸러내기  용도가 생조 → 제외 + 안내에 이름 기록
④ 용도 확인      살균·살충·균충만 통과. 나머지는 조용히 버림
⑤ 항생제 걸러내기  이름 6종 또는 작용기작 라3·라4·라5 → 제외 + 기록
⑥ 확인 필요 표시   작용기작 가4·아4 이거나 영문명에 mycin/cycline
                 → 목록에는 남기고 안내에만 적음
⑦ 값 다듬기      "수확 14일 전까지" → 14
                 "3회 이내" → 3
                 "Ⅳ급(저독성)" → "Ⅳ"
                 "Ⅱs급" → "Ⅱs"   (s를 살려 읽음)
                 원문도 함께 보관 → 잔류원문 · 횟수원문
⑧ 성분끼리 묶기   같은 작물·같은 성분이면 한 덩어리
⑨ 같은 제품 합치기 아래 7가지가 모두 같으면 한 줄로
                 상표명 · 제형 · 함량 · 대상 · 희석배수 · 잔류기간 · 사용횟수
```

**⑨의 합치기 규칙이 특히 중요합니다.**

- **제형·함량이 다르면 다른 제품입니다.** `델란 입상수화제 44%`와 `델란 수화제 75%`는
  이름이 같아도 따로 보여야 합니다.
- **판매회사만 다르면 같은 제품입니다.** 회사 이름만 모아 「농협케미컬 외 2사」로 적습니다.
- **독성 등급이 엇갈리면 더 독한 쪽을 씁니다** (`worst()`).
  덜 독한 값을 보여주는 잘못은 되돌릴 수 없으므로 안전한 쪽으로 기울였습니다.

### 6단계 — 결과 모양

```javascript
S.data = {
  apple: {
    meta: { 작물, 기준일, 출처, 샘플, 필터 },
    성분: [ { 성분명, 용도, 작용기작,
              상품: [ { 상품명, 제형, 함량, 일반명, 회사, 대상, 희석배수,
                       잔류기간, 잔류원문, 사용횟수, 횟수원문,
                       사용적기, 인축독성, 어독성 } ] } ],
    제외: { 항생제: [[이름,건수]…], 호르몬제: […], 확인필요: […] }
  },
  pear: {…}, peach: {…}, plum: {…}, persimmon: {…}
}
```

화면에 쓸 때는 `rowsOf()`가 이것을 평평한 줄 목록으로 펴 줍니다.

---

## 4. 무엇을 왜 걸러내는가

근거는 생산규정 「3-2 과수 가농인증 생산규정」입니다.

| 규정 | 앱 처리 | 코드 위치 |
|---|---|---|
| 제초제 금지 | `TQ`에서 아예 받지 않음 + `useNorm`이 제외 | `TQ`, `useNorm()` |
| 호르몬제 금지 | 용도 `생조` 제외 | `buildAll` ③ |
| 항생제 금지 | 아래 별도 설명 | `ANTIBIO`, `ANTIMOA` |
| 맹독성·고독성 금지 | 인축독성 Ⅰ·Ⅱ급 선택 불가 | `TOX` |
| 잔류기간 ×2 | 화면·출력에 `라벨 → 가농` 병기 | `phiText`, `phiFull` |

### 방제력 항목의 구조

- 시기는 `시기월`(1~12)·`시기순`(상순·중순·하순) 두 값으로 저장합니다.
  `whenText()`가 「6월 중순」 문자열로 만들고, `planKey()`가 정렬 순서를 계산합니다.
  시기를 고르면 `drawPlan()`이 이른 순으로 다시 늘어세우고 번호를 새로 매깁니다.
- **다시 그릴 때 펼쳐 둔 「자세히」 칸을 기억했다가 되살립니다.**
  전체를 다시 그리면 칸이 닫혀 입력이 끊기기 때문입니다.
  병해/해충·자재 구분을 바꿀 때는 아예 다시 그리지 않고 해당 부분만 고칩니다(`refreshHead`).
- 자재 구분은 `친환경`·`자가`·`기타` 세 가지입니다. `등록`은 조회에서 담은 항목에 자동으로 붙습니다.
  `noReg()`가 참이면 잔류기간을 적용하지 않습니다.

### 항생제 판정 — 세 겹

```javascript
// ① 성분 이름 (혼합제도 이름에 들어 있으면 걸림)
const ANTIBIO=[["가스가마이신","Kasugamycin"],["스트렙토마이신","Streptomycin"],
  ["옥시테트라사이클린","Oxytetracycline"],["옥솔린산","Oxolinic acid"],
  ["폴리옥신","Polyoxin"],["발리다마이신","Validamycin"]];

// ② 작용기작 — 농촌진흥청이 '항생물질'로 정의한 분류
const ANTIMOA=/(^|\+)\s*(라3|라4|라5)(\b|\+|$)/;
//   라3 헥소피라노실계 · 라4 글루코피라노실계 · 라5 테트라사이클린계

// ③ 감시만 — 제외하지 않음
const MOAWATCH=/(^|\+)\s*(가4|아4)(\b|\+|$)/;   // 표적 기준 분류
const ANTILOOK=/mycin|cycline|micin/i;           // 이름이 항생물질풍
```

**②와 ③을 나눈 이유가 중요합니다.**
라3·라4·라5는 농진청이 「항생물질」이라고 못 박은 분류라 새 성분이 와도 안전하게 걸러집니다.
반면 가4(DNA 토포이소메라제 저해)와 아4(키틴 생합성 저해)는 **무엇을 공격하는가**로 나눈
분류여서, 항생물질이 아닌 것이 들어올 수 있습니다.
그래서 **자동으로 지우지 않고 「확인 필요」로 표시**해 위원회가 판단하게 합니다.

> 아바멕틴·에마멕틴은 미생물에서 유래했으나 현재 화학 합성으로 생산해
> 항생물질로 보지 않습니다. 이름이 `mectin`이라 감시망에도 걸리지 않습니다.

---

## 5. 고칠 일이 생기면 어디를 보나

| 하려는 일 | 고칠 곳 | 주의 |
|---|---|---|
| 시트를 다른 것으로 바꾸기 | `SHEET_URL` | 공유를 「링크가 있는 모든 사용자·뷰어」로 |
| 작물 추가·삭제 | `CROPS`, `CROPMAP`, `TQ`의 `where C matches` | 세 곳을 함께 |
| 항생제 목록 조정 | `ANTIBIO` | 이름 일부만 적어도 됩니다 |
| 항생제 판정 기준 변경 | `ANTIMOA` / `MOAWATCH` | 자동 제외와 알림의 경계 |
| 잔류기간 배수 변경 | `phiText`, `phiFull` 및 출력부의 `d*2` | **전부 찾아 고칠 것** |
| 교구 목록 | `DIOCESE` | 규정 지역번호 순서 |
| 자재 구분 | `MAT` | 출력·엑셀도 함께 확인 |
| 해충 묶는 기준 | `PESTGRP`, `GRPORDER` | 앞에 있는 낱말부터 맞춰 봄 |
| 유기농업자재 조회 주소 | `.orglink` 안의 `href` | 국립농산물품질관리원 |
| 통합 검색 범위 | `pass()` 안의 검색 대상 열 | 현재 약제·성분·대상·회사·일반명·제형·기작 |
| 어독성 등급 순서 | `FISHORDER` | 표시 순서에만 영향 |
| 한 번에 보여줄 개수 | `S.limit`의 `50` | |
| 규정 박스 문구 | `class="regbox"` 부분 | 규정 원문과 대조 |

### 잔류기간 ×2를 바꿔야 한다면

「잔류기간」해석이 위원회에서 달리 정해질 수 있습니다. `*2`가 흩어져 있으니
**`*2`를 전부 검색**해 한꺼번에 고치십시오. 현재 여덟 곳입니다.

| 함수 | 쓰이는 곳 |
|---|---|
| `phiText()` | 화면 — 방제력 항목 도장 |
| `phiFull()` | 화면 — 조회 결과 약제 줄 |
| `drawResults()` | 화면 — 성분 카드 도장(범위 표기 `28~60`) |
| `drawPlan()` | 화면 — 자세히 보기의 「라벨 → 가농」 |
| `buildDoc()` | 인쇄물·JPG |
| `exportPlan()` | 엑셀 — 방제력 시트, 명세 시트 (두 곳) |
| `exportList()` | 엑셀 — 목록의 「가농 기준」 열 |

찾을 때 `잔류기간*2` 로만 검색하면 `d*2`·`dnum[0]*2` 형태를 놓칩니다.

### CSS를 고칠 때

`.radios`(선택 버튼) 규칙은 **`.pgrid`·`.f`보다 뒤에 있어야** 합니다.
같은 굵기의 선택자라 순서가 승부를 가릅니다. 앞으로 옮기면 버튼이 세로로 깨집니다.
파일에 그 취지를 적어 두었습니다.

---

## 6. 안전장치

`ensureData()` 안에 있습니다. **전부 실제로 작동하는지 확인된 것들입니다.**

| 상황 | 앱 동작 |
|---|---|
| 정상 | 자료를 받아 화면에 반영하고 기기에 저장 |
| **새 자료가 이전의 절반 이하** | **쓰지 않고 저장분 유지** + 빨간 띠로 건수 표시 |
| 연결 실패 | 저장해 둔 자료로 계속 작동 (과수원 통신 음영 대비) |
| 시트 비공개 | 저장분 유지 · 상태줄에 표시 |
| 빈 시트 | 저장분 유지 |
| 저장분도 없음 | 내장 샘플 + **「방제에 쓰지 마십시오」 빨간 띠** |

> **절반 기준**은 잘못된 파일을 부었을 때를 막기 위한 것입니다.
> 실제로 자료가 절반 이하로 줄 일이 생기면(작물 축소 등) 이 숫자를 조정해야 합니다.
> `n < cachedN*0.5` 부분입니다.

### 겪은 적 있는 함정

화면을 개편하며 CSS 클래스를 `wrap`→`wrap2`로 바꿨을 때,
경고 띠를 조작하는 자바스크립트가 따라가지 못해 **안전장치 전체가 죽은 적이 있습니다.**
겉으로는 멀쩡해 보였습니다. 지금은 클래스 이름 대신 `firstElementChild`로 접근합니다.
**클래스 이름을 바꿀 때는 자바스크립트도 함께 검색**하십시오.

---

## 7. 증상별 대처

### 「자료를 받지 못했습니다」

1. 시트 공유가 **「링크가 있는 모든 사용자 · 뷰어」**인지
2. 조직 관리자가 외부 공유를 막지 않았는지
3. `SHEET_URL`이 맞는지 (뒤에 `gid=`가 붙어 있으면 지우기)
4. 그래도 안 되면 시트에서 **파일 → 공유 → 웹에 게시 → .csv → 게시**
   나온 주소를 `SHEET_URL`에 넣습니다. 앱이 두 형식을 모두 알아봅니다.

### 목록이 비거나 이상하게 적음

거의 확실히 **엑셀 열 순서가 바뀐 것**입니다.
시트를 열어 A열부터 순서를 확인하고 `TQ`의 알파벳을 맞추십시오.
현재 기준: A 등록번호 · C 작물명 · E 품목명 · H 상표명 · K 용도 · R 안전사용시기 · U 회사명

### 있어야 할 약제가 안 보임

순서대로 확인하십시오.

1. 화면의 **「목록에서 제외된 …」 안내**에 그 이름이 있는지 → 규정대로 걸러진 것
2. 용도가 제초·기타인지 → `TQ`에서 아예 안 받아옴
3. 작용기작이 라3·라4·라5인지 → 항생제로 제외
4. 위 어디에도 없다면 → 원본에 그 작물로 등록돼 있는지 시트에서 직접 검색

### 「확인 필요」 줄이 떴다

**정상 작동입니다.** 항생물질일 수 있으나 판정 기준에 걸리지 않은 성분이 새로 등록된 것입니다.
앱이 임의로 지우지 않고 알린 것이니, **과수위원회에 올릴 안건**으로 보시면 됩니다.
위원회가 항생제로 판단하면 `ANTIBIO`에 이름을 추가하십시오.

### 독성 필터가 잠겨 있음

자료에 독성 등급이 하나도 없을 때 그렇게 됩니다. 시트의 인축독성·어독성 열을 확인하십시오.

### 방제력이 사라졌다는 문의

브라우저 기록을 지우거나 다른 기기·시크릿 창을 쓰면 사라집니다.
서버에 없으므로 **복구할 방법이 없습니다.** 저장 버튼을 눌러 파일로 남기도록 안내하십시오.

---

## 8. 고친 뒤 반드시 확인할 것

**규정 자료를 다루는 앱입니다. 목록이 틀리면 인증 취소와 생태계 피해로 이어집니다.**
아래를 눈으로 직접 확인하기 전에는 배포하지 마십시오.

### 최소 점검 (10분)

1. 다섯 작물을 차례로 눌러 성분 수가 나오는가
   기준값 — 사과 395 · 배 321 · 복숭아 330 · 자두 205 · 단감 98 (2026-07-27 자료)
2. 「목록에서 제외된 항생제 및 호르몬제(생조) 성분」 안내에 이름이 뜨는가
3. 아무 성분이나 펼쳐 약제 표가 나오는가
4. 담기 → 시기 입력 → 잔류기간이 「수확 ○일 전」으로 뜨는가
5. **JPG·PDF·엑셀 세 가지를 실제로 저장해 열어보는가**
6. 휴대전화 폭으로 줄여 아래 탭이 나오는가

### 자료를 크게 갱신했을 때 추가

7. 원본 엑셀에서 아무 약제나 3개 골라, 앱 화면의 희석배수·잔류기간·독성이 원본과 같은지 대조
8. 잔류기간이 「월동기」·「개화전」인 약제가 그 글자 그대로 보이는지
   (숫자가 아닌 안전사용기준이 170행 있습니다. 「제한 없음」으로 나오면 잘못된 것입니다)

### 다음 사람을 위해

고친 내용은 **커밋 메시지에 무엇을 왜 바꿨는지** 남기십시오.
GitHub의 History에서 언제든 이전 버전으로 되돌릴 수 있습니다.
크게 손볼 때는 먼저 사본 저장소에서 시험하는 편이 안전합니다.

---

## 9. 설계할 때 정한 원칙

고칠 때 이 원칙과 어긋나지 않는지 살펴 주십시오.

**앱이 규정을 해석하지 않습니다.**
판단이 갈릴 수 있는 것은 자동으로 지우지 않고 「확인 필요」로 표시해 위원회에 넘깁니다.
가4·아4를 자동 제외에서 뺀 이유가 이것입니다.

**틀린다면 안전한 쪽으로 틀립니다.**
독성 등급이 엇갈리면 더 독한 쪽을 씁니다. 덜 독하게 보여주는 잘못은 되돌릴 수 없습니다.

**보여주지 못할 바에는 원문을 그대로 둡니다.**
「월동기」를 「제한 없음」으로 바꿔 적었다가, 아무 때나 써도 된다는 뜻으로 읽힐 수 있어
원문 표기로 되돌렸습니다.

**걸러진 것을 못 걸렀다고 적지 않습니다.**
없는 위험을 만들어내면 진짜 경고까지 흘려듣게 됩니다.

**갱신은 시트 한 곳에서만.**
GitHub을 몰라도 자료를 갱신할 수 있어야 인수인계가 됩니다.

**부품을 늘리지 않습니다.**
인증키·서버·자동 수집을 모두 걷어내고 파일 두 개로 줄였습니다.
잘 돌아갈 때 편한 것보다, 고장 났을 때 원인이 보이는 것이 낫습니다.

---

## 10. 남아 있는 한계

**어독성 Ⅱs급이 실제로 오는지 확인되지 않았습니다.**
법령상 4단계(Ⅰ·Ⅱs·Ⅱ·Ⅲ)지만 2026-07-27 자료에는 Ⅱs가 한 건도 없습니다.
등록이 소멸했는지 엑셀 내보내기가 `s`를 떼는지 불분명합니다.
앱은 오면 자동으로 칸이 생기도록 대비되어 있습니다.

**표시 순서 Ⅰ→Ⅱs→Ⅱ→Ⅲ은 법령 문구를 따른 것입니다.**
업계에서는 Ⅰ·Ⅱ·Ⅱs·Ⅲ 순으로 적기도 합니다. 위원회가 달리 보면 `FISHORDER`만 고치면 됩니다.

**자료 갱신 시점을 앱이 알려주지 않습니다.**
기준일이 오래돼도 경고하지 않습니다. 필요하면 알림을 넣을 수 있습니다.

**방제력을 취합하지 않습니다.**
생산자가 파일로 저장해 교구·전국본부로 보내는 운영 방식입니다.

**공동방제력 원칙이 앱에 반영돼 있지 않습니다.**
규정상 분회별 공동 방제력이 원칙이고 개별 생산자는 분회 방제력으로 등록해야 하는데,
앱은 개인 작성만 지원합니다. 운영으로 보완해야 합니다.

---

## 인수인계 점검표

- [ ] GitHub 저장소가 **조직 계정**에 있고 **소유자가 두 명 이상**인가
- [ ] 구글 시트가 **조직 드라이브**에 있고 후임에게 편집 권한이 있는가
- [ ] 시트 공유가 「링크가 있는 모든 사용자 · 뷰어」인가
- [ ] 후임이 `README.md`의 네 단계를 직접 해 보았는가
- [ ] 앱 주소 QR코드가 분회에 배포되어 있는가
